# Expected-answer fixtures

Precomputed expected outputs for `../models/*.scm`, used by the golden tests
in §10a. Empty until Phase 0/1 (ARCHITECTURE.md §17) land — do not hand-write
numbers here; they must come from running the actual oracle/estimators
against a fixed seed.

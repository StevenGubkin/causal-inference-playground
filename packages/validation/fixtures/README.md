# Precomputed fixtures

Expected values from reference implementations (§10b), generated by
`../scripts/` and committed here so `npm run validate` runs in CI without a
Python runtime. Empty until Phase 3 (ARCHITECTURE.md §17).

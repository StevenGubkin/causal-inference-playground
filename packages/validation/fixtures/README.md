# Precomputed fixtures

Expected values from reference implementations (ARCHITECTURE.md §10b),
generated by `../scripts/` and committed here so `npm run validate` runs in
CI without a Python runtime.

- `backdoor-confounding.json` — confounding.scm vs. DoWhy/statsmodels.
- `iv-late.json` — iv-late.scm vs. linearmodels `IV2SLS`.
- `dsep-adjustment-sets.json` — backdoor-criterion validity + minimal
  adjustment sets for confounding.scm/collider.scm/mediator.scm/M-bias vs.
  networkx `is_d_separator`. Purely structural (graph topology, no
  sampling) — see `generate_dsep_fixture.py`'s docstring for why this uses
  networkx rather than dagitty.

Regenerate via `../scripts/README.md`; commit the result.

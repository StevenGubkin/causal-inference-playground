export default function App() {
  return (
    <main>
      <h1>Causal Inference Playground</h1>
      <p>
        Scaffold in progress — see <code>ARCHITECTURE.md</code> for the build
        order (§17).
      </p>
    </main>
  );
}
